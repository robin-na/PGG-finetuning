# benchmark_statistical/macro

Statistical macro simulation runs and reports live here.

Default layout:
- `benchmark_statistical/macro/runs/<run_id>/`
- `benchmark_statistical/macro/reports/<analysis_run_id>/`

The validation reference for macro RMSE and variance reports is `benchmark_statistical/data/processed_data/df_analysis_val_averaged.csv`.
