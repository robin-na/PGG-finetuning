# benchmark_statistical/micro

Statistical micro simulation runs and reports live here.

Default layout:
- `benchmark_statistical/micro/runs/<run_id>/`
- `benchmark_statistical/micro/reports/<analysis_run_id>/`

The data source for these runs is `benchmark_statistical/data`.
