# Default Reports

This directory stores analysis/report outputs derived from the original dataset root:

- `data/`
- `demographics/`

## Structure

- `micro_behavior/`
  - Analysis outputs from `Micro_behavior_eval/analysis/run_analysis.py` when evaluating source-default runs.

Use `reports/benchmark/` only for `benchmark/*` analyses.
