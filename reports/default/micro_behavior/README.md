# Default Micro Behavior Reports

This folder contains micro behavior analysis outputs for runs under:

- `outputs/default/runs/source_default/micro_behavior_eval/...`

Each subfolder is one analysis run (`<analysis_run_id>`) generated by:

```bash
python Micro_behavior_eval/analysis/run_analysis.py ...
```
